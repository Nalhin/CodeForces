import unittest
from unittest.mock import patch

from .solution_${problem_id} import Solution${problem_id}


class Solution${problem_id}Test(unittest.TestCase):
    def setUp(self):
        self.solution = Solution${problem_id}()
    % for example in examples:

    def test_${problem_id}_acceptance_${example.id}(self):
        mock_input = ${example.mock_input}
        expected = '${example.expected}'

        with patch('builtins.input', side_effect=mock_input):
            self.solution.parse_input()
            self.solution.solve()
            result = self.solution.get_result()

        self.assertEqual(result, expected)
    % endfor


if __name__ == '__main__':
    unittest.main()